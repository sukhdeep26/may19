name: "My CodeQL Analysis"

queries:
  - uses: github/codeql/codescanning-configs/automated-analysis:code-scanning.yml
  - uses: github/codeql/codescanning-configs/security-extended:code-scanning.yml

paths:
  - src/
  - include/

paths-ignore:
  - tests/
  - docs/

db-location: codeql-database
